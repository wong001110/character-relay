name: Agent expression runtime patch

on:
  push:
    branches:
      - agent/expand-expression-decisions

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - run: echo "temporary expression patch workflow is runnable"
