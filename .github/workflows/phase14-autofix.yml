name: Phase 14 One-Shot Autofix

on:
  push:
    branches: [agent/phase-14-experiment-matrix]
    paths:
      - scripts/phase14_autofix.py
      - .github/workflows/phase14-autofix.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/phase-14-experiment-matrix
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python scripts/phase14_autofix.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix: resolve phase 14 static validation"
          git push origin HEAD:agent/phase-14-experiment-matrix
