name: Phase 14 Roadmap Wording Fix

on:
  push:
    branches: [agent/phase-14-experiment-matrix]
    paths:
      - scripts/phase14_readme_fix.py
      - .github/workflows/phase14-readme-fix.yml

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/phase-14-experiment-matrix
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python scripts/phase14_readme_fix.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "docs: correct future phase wording"
          git push origin HEAD:agent/phase-14-experiment-matrix
