services:
  echo-masque:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - echo-masque-data:/data
    environment:
      CHARACTER_RELAY_ENVIRONMENT: production
      CHARACTER_RELAY_DEBUG: "false"

volumes:
  echo-masque-data:
